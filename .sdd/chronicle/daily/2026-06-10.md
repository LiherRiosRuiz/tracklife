# Cronica — 2026-06-10

### Sesion LIHER: Modulo de entrenamiento estilo Hevy para TRACKLIFE

**Que se hizo**: El usuario pidio continuar el proyecto TRACKLIFE retomando
el trabajo pendiente sobre las funcionalidades estilo Hevy App. Platon habia
diseñado el plan completo en sesion anterior (guardado en `.sdd/platon-plan.md`).
Vinci habia creado solo `tests/Traits/MongoTestCleanup.php` antes de parar.
LIHER escaneo el estado completo y orquesto la ejecucion de las Fases 0 a 5.

---

### Fase 0 — Calibracion MongoDB para tests

- `phpunit.xml` actualizado: `DB_CONNECTION=mongodb`,
  `MONGODB_URI=mongodb://mongodb:27017/tracklife_testing`,
  `MONGO_DATABASE=tracklife_testing`
- Conectividad verificada desde el contenedor `api-laravel` via `backend_net`

---

### Fase 1 — Backend: Exercise model + Seeder (60 ejercicios)

- `app/Models/Exercise.php` ampliado: nuevos campos `category`, `instructions`,
  `tips`, `image_url`, `muscles_primary`, `muscles_secondary`, `force`, `level`,
  `mechanic`, `external_id` con casts para arrays
- `app/Http/Controllers/Api/ExerciseController.php` refactorizado: filtros por
  `muscle_group`, `equipment`, `category`; busqueda regex por `q`; nuevos
  metodos `show` y `update`; semilla inline eliminada
- `routes/api.php`: rutas `GET /exercises/{id}` y `PUT /exercises/{id}`
- `database/data/exercises.json`: 60 ejercicios curados del dataset libre
  `yuhonas/free-exercise-db` (pecho, espalda, hombros, biceps, triceps,
  cuadriceps, isquios/gluteos, abdominales, trapecios, pantorrillas)
- `database/seeders/ExerciseSeeder.php`: seeder con `updateOrCreate` por
  `external_id`, imagen desde base GitHub raw
- `database/seeders/DatabaseSeeder.php`: ExerciseSeeder registrado
- **60 ejercicios sembrados en MongoDB** con imagenes e instrucciones
- Test RED→GREEN: `ExerciseTest.php` (4 tests)

---

### Fase 2 — Backend: WorkoutPlan CRUD

- `app/Models/WorkoutPlan.php`: nuevo modelo MongoDB, coleccion
  `workout_plans`, campos `user_id/name/description/days_per_week/exercises/is_public`
- `app/Http/Controllers/Api/WorkoutPlanController.php`: CRUD completo
  (index, store, show, update, destroy)
- `WorkoutController.php`: metodo `fromPlan` — devuelve workout pre-poblado
  sin persistir (patron sessionStorage)
- `routes/api.php`: rutas workout-plans CRUD + `POST /workouts/from-plan/{planId}`
  (antes de `{id}` para evitar colision de rutas)
- Test RED→GREEN: `WorkoutPlanTest.php` (7 tests: crear, listar propios, ver
  propio, no ver ajeno, actualizar, eliminar, from-plan no persiste)

**Resultado backend**: 13 tests, 41 assertions — todos pasan. Pint limpio.

---

### Fase 3 — Frontend: Biblioteca de ejercicios mejorada

- `lib/muscles.ts`: constantes `MUSCLE_GROUPS` (14 entradas, variantes con
  espacio/underscore) y `EQUIPMENT_TYPES` (9) + `muscleLabel()`, `equipmentLabel()`
- `lib/api.ts`: tipo `Exercise` ampliado + nuevos tipos `PlanSet`, `PlanExercise`,
  `WorkoutPlan`, `ActiveWorkoutSet`, `ActiveWorkout` + 7 nuevos metodos API
- `gym/ejercicios/page.tsx`: grid con imagenes, filtros pills scrollables
  por musculo y equipo, busqueda en tiempo real, links a detalle
- `gym/ejercicios/[id]/page.tsx` (nuevo): detalle — imagen con toggle
  inicio/fin, badges musculo/equipo/nivel/force, musculos primarios/secundarios,
  instrucciones numeradas

---

### Fase 4 — Frontend: Planes de entrenamiento

- `components/ExercisePickerModal.tsx`: modal bottom-sheet — busqueda +
  filtro de musculo + lista con imagen y nombre
- `planes/page.tsx`: lista de planes propios con conteo ejercicios/series
- `planes/nuevo/page.tsx`: creador de plan — picker + tabla sets por
  ejercicio (tipo Normal/Warm-up/Dropset/Fallo, peso, reps, descanso)
- `planes/[id]/page.tsx`: detalle con boton "Iniciar Workout" — llama a
  `POST /api/workouts/from-plan/{id}`, guarda en sessionStorage y navega
- `entrenamiento/page.tsx`: enlace "Planes" añadido al hub

---

### Fase 5 — Frontend: Workout activo

- `components/RestTimer.tsx`: overlay de descanso — circulo SVG animado con
  countdown, botones -15s/+15s/+30s, vibracion al finalizar
- `gym/activo/page.tsx` (nuevo): pantalla central del workout — timer de
  sesion en vivo, barra de progreso, ejercicios agrupados con tabla de sets
  (peso/reps editables, checkmark para completar), RestTimer automatico al
  completar set, persistencia en sessionStorage, boton Finalizar → `POST /api/workouts`
- `gym/page.tsx`: reemplazado — nuevo flujo con picker → preview → "Iniciar Workout"

**Resultado frontend**: `npm run lint` 0 errores. `npm run build` 42 rutas sin errores.

---

### Archivos creados (14)

1. `tests/Feature/ExerciseTest.php`
2. `tests/Feature/WorkoutPlanTest.php`
3. `app/Models/WorkoutPlan.php`
4. `app/Http/Controllers/Api/WorkoutPlanController.php`
5. `database/data/exercises.json`
6. `database/seeders/ExerciseSeeder.php`
7. `lib/muscles.ts`
8. `components/ExercisePickerModal.tsx`
9. `components/RestTimer.tsx`
10. `app/app/entrenamiento/gym/ejercicios/[id]/page.tsx`
11. `app/app/entrenamiento/planes/page.tsx`
12. `app/app/entrenamiento/planes/nuevo/page.tsx`
13. `app/app/entrenamiento/planes/[id]/page.tsx`
14. `app/app/entrenamiento/gym/activo/page.tsx`

### Archivos modificados (10)

1. `phpunit.xml`
2. `app/Models/Exercise.php`
3. `app/Http/Controllers/Api/ExerciseController.php`
4. `app/Http/Controllers/Api/WorkoutController.php`
5. `routes/api.php`
6. `database/seeders/DatabaseSeeder.php`
7. `lib/api.ts`
8. `app/app/entrenamiento/page.tsx`
9. `app/app/entrenamiento/gym/ejercicios/page.tsx`
10. `app/app/entrenamiento/gym/page.tsx`

---

### Que queda pendiente

- Tests frontend (Vitest) — `make test-setup PROJECT=web3-next` pendiente
- Paginas placeholder: `/app/entrenamiento/calendario` y `/app/entrenamiento/progreso`
  (mejorar con graficos recharts y datos reales)
- Migracion SQLite → MongoDB en API (package listo, config.php pendiente)
