# Cronica - 2026-06-29

### Sesion LIHER: Sub-sprint perfil usuario — COMPLETADO

---

### Contexto

Cierre de deuda técnica acumulada desde 2026-06-25: link a perfil en `comunidad/buscar` apuntaba a ruta 404.
El endpoint backend `GET /api/users/{id}/profile` existía pero sin página Next consumidora.
Hallazgo de seguridad durante preflight: ruta estaba FUERA del grupo `auth:sanctum` (pública por accidente).

---

### Recuperacion de estado (WSL stale → PowerShell)

Rama `feature/perfil-page` en estado desincronizado. Intento inicial de lectura via WSL `/mnt/d` retornó datos vacíos.
Recuperacion vía PowerShell (Get-Content) confirmó presencia de archivos reales y cambios en:
- `projects/web/web3-next/lib/api.ts` (modificado)
- `projects/web/web3-next/app/app/comunidad/perfil/` (directorio nuevo)

---

### Implementacion

**Backend (api-laravel):**
- Ruta `GET /api/users/{id}/profile` movida DENTRO del grupo `auth:sanctum` (fix seguridad crítico)
- `UserProfileController@show` devuelve: id, name, username, bio, avatar_url, streak_days (sin email, password, campos sensibles)
- `tests/Feature/UserProfileTest.php` — 5 tests TDD nuevos:
  - 200 estructura completa con datos esperados
  - 401 sin token Sanctum
  - 404 usuario inexistente (ObjectId inválido)
  - Verificacion: no expone email, password, macro_targets
  - Valores retornados correctos y presentes

**Frontend (web3-next):**
- `lib/api.ts` → nuevo metodo `userProfile(id)` para consumir endpoint
- `app/app/comunidad/perfil/[id]/page.tsx` — Client Component:
  - `useApiData` hook para datos autenticados
  - Skeleton loading, ErrorState (404 user not found)
  - Avatar con iniciales fallback (letra1+letra2 del name)
  - Racha (`streak_days`) destacada
  - Bio visible

**Suite final:** 84 tests / 318 assertions / 0 fallos (+5 tests nuevos vs. 79 previos)

---

### Hallazgo de seguridad y resolucion

**Problema detectado:** `UserProfileController@show` registrada en `routes/api.php` FUERA del middleware `auth:sanctum`.
Endpoint era públicamente accesible: cualquier usuario sin token podía listar perfiles.

**Incoherencia anotada:** `UserSearchController@search` (operacional desde P3.3) sí exigía auth:sanctum.
Impacto: asimetria de seguridad — busqueda gateada, perfil no.

**Decision tomada por LIHER:** Mover ruta DENTRO del grupo `auth:sanctum`. Evidencia: consumidor siempre autenticado (`comunidad/perfil/[id]` usa `useApiData` con token); busqueda ya gateada (inconsistencia obvia).

**Resultado:** Ruta ahora coherente y protegida. Tests verifican 401 sin auth.

---

### Estado del vault al cierre

| Elemento | Estado |
|----------|--------|
| Roadmap TrackLife.md | actualizado: P3.4 añadido a tabla y subseccion, deuda perfil resuelta, fecha 2026-06-29 |
| Pendientes.md | actualizado: P3.2 y P3.4 marcados `[x]`, fecha 2026-06-29 |
| .sdd/chronicle/daily/ | crónica 2026-06-29 creada |
| Suite final | 84/84 verdes, 318 assertions |
| Branch | feature/perfil-page, limpio, listo para merge |

---

## Cierre de sesion — 2026-06-29

Deuda técnica resuelta. Hallazgo de seguridad documentado y corregido. Documentacion del vault coherente.

Ver [[Roadmap TrackLife]], [[Pendientes]], [[TRACKLIFE]] para contexto completo.
