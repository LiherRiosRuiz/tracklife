# Cronica — 2026-06-08

### Sesion LIHER (10:50)

- **Turnos**: 16 mensajes
- **Temas**: Quiero que compruebes "C:\Users\Administrador\Desktop\ultimaclaude.txt"; Quiero que quevedo se encargue de documentar toda la conversación que hemos teni; Quiero que además generes con platón y vinci una manera de que también se me que; Continúa por donde te quedaste; Entonces todo el texto, lecciones aprendidas y un "historial", no se debe guarda; Pero en que ruta, yo no lo encuentro si abro obsidian; Claro, puede ser que no aparezca porque es oculta, cómo puedo habilitar en obsid; Ya lo veo todo, cuál es el comando que te tengo que dar para que guardes

---

### Sesion Platon -> Quevedo: analisis de skills pendientes

**Que se hizo**: Liher pidio a Platon un analisis de que capacidades
("skills") le faltan al ecosistema LIHER. Platon investigo `.sdd/`
(`config.yaml`, `skills.yaml`, registries, `calibration`, `memory/`) y
[[Pendientes]], y entrego un informe priorizado. Liher encargo a Quevedo
documentarlo en el vault.

**Que se decidio / encontro Platon**:
- El workspace **no usa** `.claude/skills/` — las capacidades nuevas deben
  vivir en `.sdd/skills/*.md`, targets de `Makefile`+scripts, o slash
  commands de los CLIs (`.sdd/cli/*.mjs`).
- Inventario existente: 7 skills de stack, Makefile con 20+ targets,
  `update-status.sh`/`setup.sh`/`guard.sh`, 4 agentes con slash commands.
- **Top 7 recomendaciones priorizadas** (esfuerzo Bajo/Medio):
  1. Skill MongoDB + laravel-mongodb (`mongodb-laravel.md`) — corrige
     contradiccion: el target es Mongo pero `laravel-13.md` solo ensena SQL
  2. Calibration runner (`make calibrate`) — gate Fase 2 nunca ejecutado
     (`last_run: null`)
  3. Test bootstrap frontends (`make test-setup`) — 3/4 proyectos sin
     tests, Strict TDD imposible en el 75% del codebase
  4. Skill DevOps Docker/Traefik/WSL2 (`devops-docker.md`) — guia positiva
     que falta junto a las reglas negativas del Guardian Angel
  5. Portproxy + backup MongoDB automatizados — dos puntos de fallo ya
     listados en [[Pendientes]]
  6. Slash command `/decide` en `platon.mjs` — captura decisiones como
     entidades; la memoria de Platon tiene 0 sesiones y 0 entidades
  7. Skill de seguridad (`security.md`) — patrones positivos de auth/CORS/
     CSP/validacion ausentes
- **Orden de ataque recomendado**: empezar por #1 y #6 (bajo esfuerzo,
  desbloquean sistemas inertes), luego #3 (bloquea Strict TDD).

**Que queda pendiente**: implementar las 7 recomendaciones (ninguna
ejecutada todavia — es analisis, no accion). Documentado integro en
[[Skills Pendientes]], enlazado desde [[Pendientes]], [[Platon SDD]] y
[[Home]].

---

### Recomendacion #1 (Skill MongoDB) — de analisis a implementacion

**Que se hizo**: Liher implemento directamente (sin pasar por Vinci — tarea
de contenido/documentacion bien acotada) la recomendacion #1 de
[[Skills Pendientes]]: la skill de stack MongoDB + laravel-mongodb.

**Archivos creados/modificados**:
- `.sdd/skills/mongodb-laravel.md` (236 lineas, nuevo) — mismo formato que
  las 7 skills existentes (frontmatter `skill/version/projects/triggers`).
  Cubre: trait `DocumentModel` + `$collection`, embedded documents (arrays
  casteados vs `embedsMany`/`embedsOne`), operadores de query Mongo
  (`regex`, `elemMatch`, atomicas `increment`/`push`/`pull`), indices via
  `Schema::connection("mongodb")`, transacciones (replica set / alternativa
  atomica), por que `assertDatabaseHas` con sintaxis SQL miente con
  documentos anidados/arrays/BSON y el patron correcto de testing, y
  anti-patterns especificos de Mongo+Laravel
- `.sdd/registries/api-laravel.yaml` (modificado) — anadida
  `- .sdd/skills/mongodb-laravel.md` a `skills_ref`
- `.sdd/skills.yaml` (modificado) — anadida entrada `mongodb-laravel` al
  indice (`projects: [api-laravel]`) para inyeccion automatica en el system
  prompt de Platon

**Que se decidio**: la contradiccion detectada por Platon (target Mongo,
skill existente solo SQL/Eloquent) queda resuelta. Skills de stack pasan de
7 a 8. Platon y el Guardian Angel ya disponen de patrones correctos para
la futura migracion SQLite -> MongoDB.

**Que queda pendiente**: recomendaciones #2 a #7 de [[Skills Pendientes]]
siguen abiertas; la nota se actualizo marcando #1 como completada
(2026-06-08).

---

### Cierre: las 7 recomendaciones de Skills Pendientes completadas

**Que se hizo**: Liher pidio agotar [[Skills Pendientes]] sin dejar nada
abierto ("Acaba y no dejes nada en pendiente"). Platon fallo repetidamente
(limite de sesion, timeouts), asi que LIHER asumio planificacion y
ejecucion directa de las recomendaciones #2 a #7 — tareas bien acotadas de
contenido y scripts. Con la #1 (skill MongoDB, ya documentada arriba) suman
las 7 completas en el dia.

**#2 — Calibration runner** (`make calibrate [PROJECT=...]`): creado
`.sdd/scripts/calibrate.sh`, ejecuta los 6 bloques de `calibration.checks`
(deps, test runner, lint, build, coherencia config, entorno), detecta
contenedores activos para correr tests/lint via `docker compose exec`, y
persiste `last_run`/`result`/`blockers[]`/`warnings[]` en
`calibration.status` de `config.yaml` (probado contra copia antes de
aplicar). Target + entrada en `make help` anadidos al `Makefile`.

**#3 — Test bootstrap frontends** (`make test-setup PROJECT=...`): creado
`.sdd/scripts/test-bootstrap.sh` — instala el runner recomendado por
proyecto dentro del contenedor (web1-astro: vitest; web2-nuxt:
@nuxt/test-utils+vitest; web3-next: vitest+@vitejs/plugin-react+jsdom),
genera `vitest.config.ts` especifico y un `sanity.test.ts`/`.tsx` de
ejemplo, y verifica con `npx vitest run`. api-laravel excluido (ya tiene
PHPUnit). Target + ayuda anadidos al `Makefile`.

**#4 — Skill DevOps Docker/Traefik/WSL2**: creado
`.sdd/skills/devops-docker.md` — topologia `traefik_net`/`backend_net`,
patron real de labels Traefik (extraido del compose de api-laravel),
volumenes nombrados (edge case: vacios al recrear contenedor), gestion de
.env, port forwarding WSL2->Windows->LAN, checklist de 7 edge cases de
entorno. Anadida a `.sdd/skills.yaml`, a los 4 registries y a la tabla de
triggers del Guardian Angel (`.sdd/guard/AGENTS.md`).

**#5 — Portproxy + backup MongoDB**: creados `infra/scripts/portproxy.sh`
(traduce rutas WSL->Windows con `wslpath`, lanza `wsl-portforward.ps1`
elevado via `powershell.exe -Verb RunAs`), `infra/scripts/install-portproxy-task.ps1`
(registra tarea programada "WSL2 PortForward" al inicio de sesion, sin UAC
repetido), e `infra/mongodb/backup.sh` (`mongodump --gzip --archive`,
copia a `infra/mongodb/backups/`, rotacion configurable, imprime el
`mongorestore` exacto). Targets `make portproxy`, `make portproxy-install`,
`make mongo-backup` anadidos al `Makefile`. Creado directorio
`infra/mongodb/backups/`.

**#6 — `/decide`**: anadidas `saveDecision()` y el comando `/decide [titulo]`
en `.sdd/cli/platon.mjs` — encadena preguntas (contexto, decision, razon,
alternativas descartadas) igual que `/end`, y persiste una entidad con
frontmatter `type: decision` en `.sdd/memory/entities/decision-<slug>.md`.
Anadido a `showHelp()`; sintaxis verificada con `node --check`.

**#7 — Skill de seguridad**: creado `.sdd/skills/security.md` — patron
Sanctum + tokens en SPA tal como esta en TRACKLIFE (AuthProvider/AuthGuard),
analisis de por que el token vive en localStorage y mitigaciones (CSP,
sanitizacion, expiracion+revocacion), CORS, CSP via Traefik, doble
validacion Zod+FormRequest, gestion de `.env` y rotacion de secretos.
Anadida a `.sdd/skills.yaml`, los 4 registries y la tabla de triggers del
Guardian Angel.

**Archivos creados** (8): `.sdd/skills/{devops-docker.md, security.md}`
(mongodb-laravel.md ya contado en la entrada anterior),
`.sdd/scripts/{calibrate.sh, test-bootstrap.sh}`,
`infra/scripts/{portproxy.sh, install-portproxy-task.ps1}`,
`infra/mongodb/backup.sh`, directorio `infra/mongodb/backups/`.

**Archivos modificados**: `Makefile` (6 targets nuevos + ayuda),
`.sdd/skills.yaml`, `.sdd/cli/platon.mjs`, `.sdd/guard/AGENTS.md`, los 4
`.sdd/registries/*.yaml`, `docs/Skills Pendientes.md` (las 7 recomendaciones
marcadas COMPLETADO con detalle de entrega).

**Que se decidio**: las 7 recomendaciones de [[Skills Pendientes]] quedan
COMPLETADAS (2026-06-08). Skills de stack pasan de 7 a 10. El ciclo
analisis -> implementacion se cerro en el mismo dia.

**Que queda pendiente — accion humana de Liher**:
- Ejecutar `make portproxy-install` con permisos de Administrador (registra
  la tarea programada de Windows; requiere elevacion una sola vez)
- Ejecutar `make calibrate` y `make test-setup PROJECT=<web1-astro|web2-nuxt|web3-next>`
  con el stack Docker arriba (`make up`) — los scripts detectan contenedores
  parados y no pueden correr tests/lint sin ellos
- Tras la primera ejecucion exitosa de `test-setup`, actualizar
  `test_ready: true` en `calibration.status` de `config.yaml` donde aplique
- Primera corrida manual de `make mongo-backup` para verificar el flujo de
  backup/restore antes de confiar en el de forma rutinaria

---
