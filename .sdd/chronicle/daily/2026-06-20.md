# Cronica — 2026-06-20

## Sesion: Indices MongoDB + Refactor DashboardController (Paso 3)

**Objetivo**: Tercer paso del plan de mejora de TrackLife — infra de base de datos y limpieza arquitectonica.

**Contexto**:
- Ninguna coleccion de MongoDB tiene indices declarados. Todas las queries filtran por `user_id` como minimo — sin indice es un full collection scan en cada request.
- `DashboardController::index()` llama a `app(MacroController::class)->dailyProgress($request)` — un controller invocando a otro controller directamente, violacion del patron del workspace.
- Con 5 tests de Feature en verde (AuthTest, ExerciseTest, WorkoutPlanTest, MealTest, MacroTest), el refactor puede hacerse con confianza.

**Tareas de esta sesion**:
1. Crear migracion de indices MongoDB para todas las colecciones principales
2. Extraer `MacroService::dailyProgress()` del MacroController
3. Refactorizar DashboardController para usar el nuevo MacroService
4. Verificar que `php artisan test tests/Feature` sigue en verde

**Stack**: Laravel 13 · MongoDB · php artisan migrate

---

## Resultado: Indices MongoDB + Refactor DashboardController

**Output de `php artisan migrate`:**
```
Creating migration table ......................................  27.90ms DONE
0001_01_01_000000_create_users_table ..........................  79.33ms DONE
0001_01_01_000001_create_cache_table ..........................  47.62ms DONE
0001_01_01_000002_create_jobs_table ...........................  62.52ms DONE
2026_06_19_223059_add_indexes_to_mongodb_collections ......... 196.82ms DONE
```

**Output de `php artisan test tests/Feature`:**
```
Tests: 38 passed (150 assertions)
Duration: 3.98s
```

**Archivos creados/modificados:**
- `app/Services/MacroService.php` — CREADO
- `app/Http/Controllers/Api/MacroController.php` — MODIFICADO (usa MacroService por constructor)
- `app/Http/Controllers/Api/DashboardController.php` — MODIFICADO (elimina anti-patron app(MacroController::class))
- `database/migrations/2026_06_19_223059_add_indexes_to_mongodb_collections.php` — CREADO

**Indices MongoDB anadidos:**
- `meal_entries`: `user_id`, `[user_id, date]`
- `workouts`: `user_id`, `[user_id, date]`
- `workout_plans`: `user_id`
- `activities`: `user_id`, `[user_id, date]`
- `biometric_readings`: `user_id`
- `social_posts`: `user_id`
- `wearable_connections`: `user_id`
- `products`: `barcode` (sparse)

**Decisiones tecnicas:**

1. **MacroService**: firma `dailyProgress(User $user, string|null $date = null)`. El `null` resuelve a `Carbon::today()` dentro del service — mismo comportamiento que antes.

2. **MacroController**: elimina imports de `MealEntry` y `Carbon`, inyecta `MacroService` por constructor. Comportamiento externo identico.

3. **DashboardController**: anade `MacroService` al constructor (que ya tenia `CoachService` y `FeedService`). Elimina el `app(MacroController::class)` — anti-patron resuelto.

4. **Migracion down()**: usa `dropIndexIfExists` en lugar de `dropIndex` — mas seguro ante re-ejecuciones.

5. **Primera migracion del proyecto**: la tabla `migrations` no existia — artisan la creo automaticamente.

**Estado de cobertura API:** 5/18 controllers con tests. Suite completa: 38 tests, 150 assertions, 0 failed.

**Pusheado a GitHub:** rama `master`, 4 archivos — MacroService, MacroController, DashboardController, migracion de indices.

---

## Sesion: P1.1 — Pagina de Ajustes (frontend)

**Objetivo**: Primera de las 9 paginas placeholder del frontend de TrackLife. La pagina `/ajustes` existe pero es estatica — sin conexion a la API.

**Contexto**:
- Archivo actual: `projects/web/web3-next/app/app/ajustes/page.tsx`
- Endpoint disponible: `UserProfileController` en la API (GET y PUT /api/profile)
- La pagina debe permitir: editar nombre, email, username, configuracion de privacidad
- Stack: Next.js 16 · React 19 · TypeScript · Tailwind 4 · "use client"

---

## Resultado: P1.1 — Pagina de Ajustes

**Archivo modificado:** `projects/web/web3-next/app/app/ajustes/page.tsx`
**`api.ts`:** sin cambios — `api.updateProfile` ya existia (linea 267)

**Implementado:**
- Seccion **Perfil**: formulario con `name` y `bio` editables → `PUT /api/profile`
- Seccion **Privacidad**: 5 toggles (`meals`, `product_scans`, `progress_photos`, `biometrics`, `workouts`) con opciones public/followers/private → `PUT /api/profile` con `privacy_settings`
- Seccion **Cuenta**: email display-only + boton cerrar sesion (revoca token en servidor)
- Dos formularios independientes — Perfil y Privacidad se guardan por separado
- Estados: loading inicial, submitting, mensaje de exito/error por seccion

**Decisiones tecnicas:**
1. **Patron inner component** (`AjustesForm` con props no-null) — evita violacion de `react-hooks/set-state-in-effect`, lint pasa limpio
2. **`username` y `email` son display-only** — `UserProfileController.update()` no los acepta
3. **Campos de privacidad** tomados exactamente de `User::defaultPrivacySettings()` — sin inventar campos
4. **Logout** llama al `logout()` del AuthContext (revoca token en servidor) y redirige a `/auth/login`

**Paginas placeholder completadas: 1/9**
**Pusheado a GitHub:** rama `master`, commit `feat: pagina ajustes conectada a API`

---

## Sesion: P1.2 — Pagina de Calendario (frontend)

**Objetivo**: Segunda de las 9 paginas placeholder. La pagina `/entrenamiento/calendario` existe pero es estatica.

**Contexto**:
- Archivo actual: `projects/web/web3-next/app/app/entrenamiento/calendario/page.tsx`
- Endpoints disponibles: `GET /api/workouts` (historial) y `GET /api/meals` (diario, filtrable por fecha)
- La pagina debe mostrar: vista semanal navegable con indicadores de entrenamientos y comidas por dia
- Stack: Next.js 16 · React 19 · TypeScript · Tailwind 4

---

## Resultado: P1.2 — Pagina de Calendario

**Archivo modificado:** `projects/web/web3-next/app/app/entrenamiento/calendario/page.tsx`
**`api.ts`:** sin cambios — `api.workouts` y `api.meals` ya existian

**Implementado:**
- Vista semanal lunes-domingo con navegacion anterior/siguiente + boton "Hoy"
- Cada dia: indicador verde si hay workout, indicador si hay comidas, dia actual resaltado
- Panel de detalle al hacer click en un dia: workouts (nombre, series, volumen, duracion) + comidas (tipo, kcal, proteina)
- 7 llamadas paralelas para meals (una por dia), 1 fetch para workouts con filtro en cliente
- Auth guard: `{ enabled: !!token }` para evitar fetch antes de hidratacion del AuthProvider
- TypeScript compila sin errores

**Decisiones tecnicas:**
1. **Fechas locales** — `getFullYear/getMonth/getDate` en lugar de `toISOString().slice(0,10)` — evita UTC shift en usuarios con offset positivo
2. **Lunes correcto** — `dow === 0 ? -6 : 1 - dow` para tratar domingo (0) correctamente
3. **Meals: 7 llamadas paralelas** — `MealController::index` solo acepta un dia por peticion, se resuelven con `Promise.all`
4. **Workouts: fetch unico + filtro cliente** — el endpoint devuelve los ultimos 30, se filtra por fecha en cliente
5. **Panel toggle** — click en el mismo dia cierra el panel de detalle

**Paginas placeholder completadas: 2/9**
**Pusheado a GitHub:** rama `master`

---

## Sesion: P1.3 — Pagina Plan Nutricional (frontend)

**Objetivo**: Tercera de las 9 paginas placeholder. `/nutricion/plan` existe pero tiene el plan hardcodeado en el frontend sin persistencia.

**Contexto**:
- Archivo actual: `projects/web/web3-next/app/app/nutricion/plan/page.tsx`
- Endpoint disponible: `MacroController` — `GET /api/macros/targets` y `PUT /api/macros/targets`
- La pagina debe permitir ver y editar objetivos de macros (calorias, proteina, carbs, grasa) con persistencia real en MongoDB
- Stack: Next.js 16 · React 19 · TypeScript · Tailwind 4

---

## Resultado: P1.3 — Pagina Plan Nutricional

**Archivos modificados:**
- `projects/web/web3-next/app/app/nutricion/plan/page.tsx` — reescritura completa (322 lineas)
- `projects/web/web3-next/lib/api.ts` — funcion `getMacroTargets` anadida

**Implementado:**
- Formulario de objetivos: calorias, proteina, carbohidratos, grasa — con persistencia via `PUT /api/macros/targets`
- Componente `MacroDistribution` independiente — recalcula distribucion calorica en tiempo real (proteina 4kcal/g, carbs 4kcal/g, grasa 9kcal/g) sin debounce
- Aviso de coherencia: si la suma calorica de macros difiere >100 kcal del objetivo, se muestra aviso amarillo
- 3 cards de referencia rapida (perder peso / mantener / ganar masa) con valores preset
- Validacion cliente: calorias minimo 800, macros >= 0
- Grid 2 columnas en inputs (`sm:grid-cols-2`)
- Estado del formulario: `"idle" | "saving" | "ok" | "error"` — coherente con ajustes

**Decisiones tecnicas:**
1. **Patron inner-component** (`PlanForm`) — evita violacion `react-hooks/set-state-in-effect`, mismo diseno que `ajustes/page.tsx`
2. **Carga de targets via `useApiData`** con guard `{ enabled: !!token }` — en caso de error API se muestran defaults
3. **`getMacroTargets` en `api.ts`** — funcion nueva; `updateMacroTargets` ya existia
4. **`MacroDistribution` desacoplado** — recibe valores como props, no accede al estado del formulario directamente

**Push GitHub:** SHA `87d693ad` (page.tsx) + `1f6f2007` (api.ts), rama `master`

**Paginas placeholder completadas: 3/9** (ajustes, calendario, plan nutricional)

---

## Sesion: P1.4 — Pagina Favoritos Nutricionales (frontend)

**Objetivo**: Cuarta de las 9 paginas placeholder. `/nutricion/favoritos` — lista de alimentos y recetas marcados como favoritos por el usuario.

**Stack**: Next.js 16 · React 19 · TypeScript · Tailwind 4

---

## Resultado: P1.4 — Pagina Favoritos Nutricionales

**Archivo modificado:** `projects/web/web3-next/app/app/nutricion/favoritos/page.tsx` — reescritura completa (430 lineas)
**`api.ts`:** sin cambios

**Implementado:**
- Lista de favoritos (alimentos y recetas) persistida en `localStorage` bajo la clave `tracklife_favorites`
- Candidatos de alimentos: extraidos del historial de `api.meals`, deduplicados por nombre, ordenados por frecuencia de uso
- Candidatos de recetas: `api.recipes` completo
- Seccion "Mas usados" — top 5 sin estrella — sugerencia visual para marcar como favorito
- Modal "+ Diario" real: llama `api.createMeal` con tipo de comida seleccionable (desayuno/almuerzo/merienda/cena/snack)
- Estado vacio diferenciado: "Sin favoritos" (lista vacia) vs "Sin coincidencias para X" (busqueda sin resultados)

**Decisiones tecnicas:**
1. **Sin endpoint de favoritos** — `/api/favorites` no existe; persistencia en `localStorage` con comentario `// TODO: replace with API endpoint when available`
2. **Candidatos deduplicados** — alimentos extraidos del historial de comidas, deduplicados por nombre y ordenados por frecuencia de uso
3. **Modal de comida seleccionable** — tipo desayuno/almuerzo/merienda/cena/snack via `api.createMeal`
4. **Estado vacio diferenciado** — distincion entre lista vacia real y busqueda sin coincidencias

**Push GitHub:** SHA `1d2416631c9353699d094c15f3ab94d458909fde`, rama `master`

**Paginas placeholder completadas: 4/9** (ajustes, calendario, plan nutricional, favoritos)

---

## Sesion: P1.5 — Pagina Buscar Comunidad (frontend)

**Objetivo**: Quinta de las 9 paginas placeholder. `/comunidad/buscar` — busqueda de usuarios y coaches dentro de la plataforma.

**Stack**: Next.js 16 · React 19 · TypeScript · Tailwind 4

---

## Resultado P1.5 — Pagina Buscar Comunidad

**Archivo modificado:** `projects/web/web3-next/app/app/comunidad/buscar/page.tsx` — reescritura completa (182 lineas)
**`api.ts`:** sin cambios

**Implementado:**
- Busqueda client-side sobre datos del feed (`GET /api/feed`) — `/api/users/search` no existe
- Usuarios deduplicados del feed por `id`, ordenados por aparicion mas reciente
- Debounce 300ms con `useEffect` + `setTimeout`, limpieza con `clearTimeout` en cleanup
- Busqueda activa solo con >= 2 caracteres; estado inicial muestra "Activos recientemente"
- `UserCard`: iniciales como fallback si no hay `avatar_url`; enlace "Ver perfil" a `/app/comunidad/perfil/:id`

**Decisiones tecnicas:**
1. **Sin endpoint de busqueda** — `GET /api/users/search` no existe; busqueda sobre feed con comentario `// TODO backend`
2. **Sin tabs Usuarios/Coaches** — el modelo `User` no expone campo `role`
3. **Debounce 300ms** — evita queries en cada keystroke; patron identico al usado en ejercicios

**Push GitHub:** SHA `a39afebdfdaa6ed64129e4b70306a7ead3d28046`, rama `master`

**Paginas placeholder completadas: 5/9** (ajustes, calendario, plan nutricional, favoritos, comunidad/buscar)

---

## Sesion: P1.6 — Pagina Coach Plan (frontend)

**Objetivo**: Sexta de las 9 paginas placeholder. `/coach/plan` — plan de entrenamiento y nutricion generado/gestionado por el coach IA.

**Stack**: Next.js 16 · React 19 · TypeScript · Tailwind 4

---

## Resultado P1.6 — Pagina Coach Plan

**Archivo modificado:** `projects/web/web3-next/app/app/coach/plan/page.tsx` — reescritura completa (303 lineas)
**`api.ts`:** sin cambios

**Implementado:**
- Seccion "Tu objetivo": muestra `target_weight`, `target_body_fat` y `deadline` del contexto de auth (`user.transformation_goal`); dias restantes calculados en `useMemo([deadline])`
- Plan semanal estatico (Lunes fuerza / Martes cardio / ...) con comentario `// TODO: endpoint futuro` — `/api/coach/plan` no existe
- Insights del coach clasificados por tipo (`nutrition` / `training` / `cardio` / `recovery`) con icono y color por severidad (`warning` = amarillo, `success` = verde, `info` = azul) — datos de `GET /api/coach/daily` (`insights[]`)
- Seccion nutricional: `MacroBar` con consumo real del dia si `api.macroProgress` responde; si no, muestra targets en texto

**Decisiones tecnicas:**
1. **Sin `/api/coach/plan`** — solo existe `/api/coach/daily` (devuelve `insights[]`); plan semanal estatico con TODO marcado
2. **Dias restantes en `useMemo`** — dependencia `[deadline]` para evitar re-renders en cada render
3. **Clasificacion de insights** — switch por `insight.type`, fallback a icono generico; severidad mapea a clase Tailwind de color
4. **MacroBar condicional** — si `api.macroProgress` falla o devuelve vacio, degradacion a texto de targets; sin estado de error visible

**Push GitHub:** SHA `ae8cc431c0a1e18f17da1beb39bd66fc9b2f0e6a`

**Paginas placeholder completadas: 6/9** (ajustes, calendario, plan nutricional, favoritos, comunidad/buscar, coach/plan)

---

## Sesiones P1.7 + P1.8 + P1.9 — Ejecutadas en paralelo

**Objetivo**: Cerrar las tres paginas restantes del sprint P1 de forma simultanea.

---

## Resultado: P1.7 — Pagina Coach Insights

**Archivo modificado:** `projects/web/web3-next/app/app/coach/insights/page.tsx` — reescritura completa
**`api.ts`:** sin cambios

**Implementado:**
- Resumen por severidad: pills condicionales con conteo de avisos, exitos e informativos
- Filtro por categoria: 5 pills client-side (Todos / Nutricion / Entrenamiento / Cardio / Recuperacion)
- Reutiliza exactamente `INSIGHT_COLORS` e `INSIGHT_ICONS` de `coach/plan/page.tsx` — sin duplicacion

**Push GitHub:** SHA `cfcbd720`

**Paginas placeholder completadas: 7/9**

---

## Resultado: P1.8 — Pagina Biometricos Cuerpo

**Archivos modificados:**
- `projects/web/web3-next/app/app/biometricos/cuerpo/page.tsx` — reescritura completa (elimina `alert()`)
- `projects/web/api-laravel/app/Http/Controllers/Api/BiometricController.php` — fix: `muscle_mass` anadido al enum de validacion (faltaba; causaria 422 en runtime)

**Implementado:**
- 3 inputs (peso / grasa / musculo); `Promise.all` guarda en paralelo solo los campos con valor
- Historial 90 dias con deltas por tipo: flecha arriba / abajo / igual
- Resumen: ultima medicion de cada tipo
- Refetch automatico tras guardar

**Decisiones tecnicas:**
1. Fix preventivo en `BiometricController` — `muscle_mass` no estaba en el enum de validacion; detectado antes de que causara errores en produccion
2. `Promise.all` para los campos con valor — ahorra round-trips innecesarios

**Push GitHub:** SHAs `987a3355` (page) + `80044005` (controller)

**Paginas placeholder completadas: 8/9**

---

## Resultado: P1.9 — Pagina Entrenamiento Progreso

**Archivo modificado:** `projects/web/web3-next/app/app/entrenamiento/progreso/page.tsx` — reescritura completa
**`api.ts`:** sin cambios — reutiliza `api.workouts()` existente

**Implementado:**
- 4 tarjetas resumen: total entrenamientos, volumen total, racha actual, semana actual
- Evolucion semanal: ultimas 8 semanas con `MacroBar` y porcentaje de cambio semana a semana
- Lista de ultimos 10 entrenamientos expandible con series por ejercicio
- Top 5 ejercicios por frecuencia de aparicion
- Todo calculado en `useMemo` desde los datos de `api.workouts()` — sin endpoints adicionales

**Push GitHub:** SHA `13d90bb7`

**Paginas placeholder completadas: 9/9**

---

## Sprint P1 — COMPLETADO

**9/9 paginas placeholder implementadas:**

| # | Pagina | SHA |
|---|--------|-----|
| P1.1 | ajustes | feat: pagina ajustes conectada a API |
| P1.2 | entrenamiento/calendario | — |
| P1.3 | nutricion/plan | `87d693ad` + `1f6f2007` |
| P1.4 | nutricion/favoritos | `1d241663` |
| P1.5 | comunidad/buscar | `a39afebdf` |
| P1.6 | coach/plan | `ae8cc431` |
| P1.7 | coach/insights | `cfcbd720` |
| P1.8 | biometricos/cuerpo | `987a3355` + `80044005` |
| P1.9 | entrenamiento/progreso | `13d90bb7` |

**Archivos modificados en P1.7–P1.9:**
- `app/app/coach/insights/page.tsx`
- `app/app/biometricos/cuerpo/page.tsx`
- `app/app/entrenamiento/progreso/page.tsx`
- `app/Http/Controllers/Api/BiometricController.php`

**Estado final:** stack completo operativo. 38 tests, 150 assertions, 0 failed.

**Proxima fase — P2:**
- Form Requests en la API Laravel (validacion centralizada)
- Validacion Zod en frontend
- Server Components en dashboard
- Redesign landing TRACKLIFE

---

## Sprint P2 — Inicio

**Objetivo**: Siguiente fase de mejora de TrackLife tras completar P1 (9/9 placeholders). El usuario ha dado luz verde sin restricciones.

**Ejes de P2:**
- P2.1: Form Requests en Laravel API (validacion robusta server-side)
- P2.2: Validacion Zod en frontend Next.js
- P2.3: Landing redesign (Astro 6 + Tailwind 4 + SEO)
- P2.4: API Resources en Laravel (transformacion consistente de respuestas)
- P2.5: Dashboard con datos reales mas completos

**Estado al entrar**: App operativa en http://app.tracklife.test · Usuario test activo · 9/9 placeholders implementados · GitHub sincronizado

---
