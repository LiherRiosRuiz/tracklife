# Cronica - 2026-06-25

### Sesion LIHER: Sprint P3.1 — Tests de API (TDD) — COMPLETADO

---

### Contexto

Continuacion del proyecto tracklife. Sesion de Orientacion (apertura) detectó baseline
en 44/44 tests verdes. Objetivo: P3.1 — Tests de API (TDD) completos.

---

### Resultado verificado

**Suite de tests final:**
```
74 tests / 274 assertions / 0 fallos
(+30 tests nuevos vs. baseline 44)
```

**Archivos creados en `tests/Feature/`:**
- `WorkoutTest.php` — 8 tests TDD nuevos
- `BiometricTest.php` — 9 tests TDD nuevos
- `ActivityTest.php` — 8 tests TDD nuevos

**Archivo ampliado:**
- `AuthTest.php` — +5 tests → 14 total
  - Validacion: short password, email invalido, login sin credenciales
  - Auto-generacion username desde email
  - Sufijo en username duplicado

**Archivos reformateados (Pint — solo whitespace):**
- `DashboardTest.php`
- `MacroTest.php`
- `MealTest.php`

---

### Decisiones tecnicas

1. **Sprint TEST-ONLY**: cero cambios a controladores, Requests, Resources, modelos,
   rutas. La suite pasa sin modificaciones de produccion — no había bugs latentes.

2. **Correccion Vinci**: `assertJsonPath()` debe encadenarse sobre `TestResponse`,
   no sobre `$this`. Ajuste confirmado en primer test (trabajo limpio).

3. **Gap de alcance documentado**: `BiometricController::today()` NO calcula "deltas"
   (menciona el roadmap). Quedan fuera de P3.1. Si se quieren, abrir sub-sprint
   feature aparte.

---

### Incidencia anotada (fuera de scope P3.1)

**web1-astro crash-loop:**
- Contenedor en estado `Restarting` desde apertura de P3.1
- Probables causas: dependencia faltante, error en build, incompatibilidad Astro 6
- Decision: diferir diagnostico a sesion dedicada
- Impacto: landing no accesible, app funcional

---

### Estado del workspace al cierre P3.1

| Componente | Estado |
|------------|--------|
| Test suite | 74/74 verdes, 274 assertions |
| Controladores cubiertos | WorkoutController, BiometricController, ActivityController, AuthController |
| Code coverage | Feature tests completos — sin tests unitarios (decision por TDD workflow) |
| Branch | master, clean, pendiente push final |
| Docker | 3/4 contenedores activos (web1-astro fault) |
| Vault | 23 notas, actualizado con P3.1 cierre |

---

### Proximos pasos

1. **P3.2**: Server Components en Dashboard (Next.js optimization)
2. **P3.3**: Búsqueda real de usuarios (GET /api/users/search endpoint)
3. **Gap**: decidir si implementar deltas biometric en P3 o aplazar a P4

Ver [[Roadmap TrackLife]] para detalles del plan.

