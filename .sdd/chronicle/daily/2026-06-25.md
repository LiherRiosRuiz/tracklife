# Cronica - 2026-06-25

### Sesion LIHER: Apertura de P3.1 — Tests de API (TDD)

---

### Contexto

El usuario solicito "continuar con el proyecto tracklife". Al establecerse la sesion,
se detectaron dos incidencias durante la orientacion inicial:

1. **Desorientacion de workspace**: `/mnt/d/` en WSL2 mostraba directorio vacio. Resolucion: acceso directo via ruta Windows `D:\Compartida\LIHER` desde PowerShell, donde reside el vault real (23 notas, 59k archivos).

2. **Estado del contenedor landing**: `web1-astro` (Landing TRACKLIFE) en estado `Restarting` / crash-loop. Detectado pero fuera del scope de P3.1; anotado para decision posterior.

---

### Baseline verificado

- Sprints P1 y P2 cerrados (ultima sesion: 2026-06-21)
- Test suite: **44/44 tests verdes, 170 assertions**
- Contenedores Docker activos (excepto web1-astro)
- Siguiente item del roadmap: **P3.1 — Tests de API (TDD)**

---

### Plan de sesion P3.1

Implementacion de cobertura de tests en controladores de API Laravel usando strict TDD:

**Controladores objetivo:**
- `WorkoutController` — mínimo 5 tests
- `BiometricController` — mínimo 5 tests
- `ActivityController` — mínimo 5 tests
- `AuthController` — revisión y ampliación del existente

**Stack:** Laravel 13 · MongoDB · Trait `MongoTestCleanup` (evita `RefreshDatabase` incompatible)

**Metodologia:** RED → GREEN → REFACTOR en cada test. Commits frecuentes tras cada ciclo.

---

### Archivos identificados

| Archivo | Ubicacion | Estado |
|---------|-----------|--------|
| `WorkoutTest.php` | `tests/Feature/` | A CREAR |
| `BiometricTest.php` | `tests/Feature/` | A CREAR |
| `ActivityTest.php` | `tests/Feature/` | A CREAR |
| `AuthTest.php` | `tests/Feature/` | EXISTENTE (revisar) |
| `MongoTestCleanup` trait | `tests/` | EXISTENTE |

---

### Incidencia anotada (fuera de scope P3.1)

**web1-astro crash-loop:**
- Contenedor en estado `Restarting`
- Probables causas: dependencia faltante, error en build, incompatibilidad Astro 6
- Decision: diferir diagnostico a sesion dedicada (P?.?)
- Impacto actual: landing no accesible, app funcional

---

### Proximos pasos

1. Sesion Platon (Fase 1): Detectar y calibrar WorkoutController, BiometricController, ActivityController
2. Sesion Vinci: Crear tests RED → GREEN → REFACTOR
3. Ejecutar `php artisan test tests/Feature` tras cada test nuevo
4. Registrar resultado en cronica de sesion siguiente

---

### Estado del workspace al cierre de orientacion

| Componente | Estado |
|------------|--------|
| Vault Obsidian | 23 notas, 122 enlaces, accesible |
| Test suite | 44/44 verdes |
| Docker | 3/4 contenedores activos (web1-astro fault) |
| Branch | master, clean |
| Roadmap | P3.1 listo para inicio |

