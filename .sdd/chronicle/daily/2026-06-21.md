# Cronica — 2026-06-21

### Sesion LIHER: Sprint P2 completado

---

### P2.1 — Form Requests Laravel

- Creados 20 Form Requests en `app/Http/Requests/`
- 12 controladores refactorizados: 0 llamadas `$request->validate()` inline
- Tests: 39/39 verdes

---

### P2.2 — Zod Frontend (web3-next)

- Instalado `zod@4.4.3`
- Creado `lib/schemas.ts` con schemas para todos los modelos
- Formularios validados: login, registro, registrar comida, plan nutricional
- Ajuste clave: Zod v4 usa `error:` en lugar de `errorMap:` — corregido

---

### P2.3 — Landing Redesign (web1-astro)

- Creados: `src/layouts/Layout.astro`, `src/styles/global.css`
- Modificados: `astro.config.mjs` (Tailwind 4 Vite, sitemap, site URL),
  `index.astro`, `como-funciona.astro`, `precios.astro`
- Hero impactante, stats, features grid, steps, CTA final
- Meta tags OG/Twitter, canonical URL, sitemap generado
- Build: 3 paginas en 4.33s sin errores

---

### P2.4 — API Resources Laravel

- Creados 8 Resources: `UserResource`, `MealResource`, `WorkoutResource`,
  `BiometricResource`, `ActivityResource`, `ExerciseResource`,
  `RecipeResource`, `WorkoutPlanResource`
- Todos los controladores devuelven Resources, no modelos raw
- Contrato establecido: `_id → id`, `user_id` no expuesto, fechas ISO 8601
- Fix test: `MealTest` assertaba `user_id` — cambiado a verificar
  ausencia de `user_id` y presencia de `id`
- Tests: 39/39 verdes

---

### P2.5 — Dashboard Mejorado

**Backend:**
- `MacroService::weeklyCalories()`: ultimos 7 dias agrupados por fecha
- `DashboardController`: extendido con `weekly_calories` y `recent_workouts`
- `tests/Feature/DashboardTest.php`: 5 tests TDD nuevos

**Frontend:**
- `components/WeeklyChart.tsx`: BarChart Recharts con datos semanales
- `app/app/page.tsx`: streak destacado + grafico semanal + workouts recientes

**Correcciones TDD durante implementacion:**
- `RefreshDatabase` incompatible con MongoDB → sustituido por `MongoTestCleanup`
- Cast `(float)` necesario para aserciones JSON con valores decimales
- Query Carbon ajustada para comparar ISODate de MongoDB

**Resultado final:** 44/44 tests verdes, 170 assertions. Build Next.js limpio.

---

### GitHub

Todos los cambios pusheados a `LiherRiosRuiz/tracklife` · rama `master`.

---

### Estado del stack tras Sprint P2

| Capa | Estado |
|------|--------|
| API Laravel | 44 tests · 20 Form Requests · 8 Resources · 5 Services |
| App Next.js | Zod 4.4.3 · WeeklyChart · Dashboard enriquecido |
| Landing Astro | Redesign completo · Tailwind 4 · SEO (OG, sitemap) |

---

### Que queda pendiente

- Paginas calendario y progreso con datos reales
- Tests frontend Vitest
- Migracion SQLite → MongoDB en API (config pendiente)
- PWA / app movil
- Providers wearables reales (Zepp, Whoop OAuth)
