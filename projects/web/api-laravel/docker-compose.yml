services:
  api-laravel:
    build: .
    container_name: api-laravel
    restart: unless-stopped
    volumes:
      - .:/app
      - api_vendor:/app/vendor
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_NAME: TRACKLIFE
      DB_CONNECTION: mongodb
      MONGO_HOST: mongodb
      MONGO_PORT: 27017
      MONGO_DATABASE: ${MONGO_DB:-tracklife}
      MONGO_USERNAME: ${MONGO_USER:-admin}
      MONGO_PASSWORD: ${MONGO_PASSWORD:-changeme_in_production}
      MONGODB_URI: mongodb://${MONGO_USER:-admin}:${MONGO_PASSWORD:-changeme_in_production}@mongodb:27017/${MONGO_DB:-tracklife}?authSource=admin
    networks:
      - traefik_net
      - backend_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=Host(`api.${DOMAIN:-tracklife.test}`)"
      - "traefik.http.routers.api.entrypoints=web,websecure"
      - "traefik.http.routers.api.tls=${API_TLS_ENABLED:-false}"
      - "traefik.http.services.api.loadbalancer.server.port=8000"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolalloworiginlist=http://app.${DOMAIN:-tracklife.test},http://${DOMAIN:-tracklife.test},http://www.${DOMAIN:-tracklife.test},https://app.${DOMAIN:-tracklife.test},https://${DOMAIN:-tracklife.test},https://www.${DOMAIN:-tracklife.test}"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolallowheaders=Content-Type,Authorization"
      - "traefik.http.routers.api.middlewares=api-cors,sec-csp-api@file"

volumes:
  api_vendor:

networks:
  traefik_net:
    external: true
  backend_net:
    external: true
